# Versions aligned with backend/requirements.lock (see repo). torch is pinned
# to the CPU build - the +cpu local version only exists on the PyTorch CPU
# index, so this can never silently resolve to the multi-GB CUDA wheel.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.12.1+cpu
einops==0.8.2
numpy==2.5.1
pandas==3.0.3
tqdm==4.68.3
huggingface_hub==1.22.0
safetensors==0.8.0
sentence-transformers==5.6.0
transformers==5.13.0
tokenizers==0.22.2
scikit-learn==1.9.0
scipy==1.18.0
fastapi==0.139.0
uvicorn==0.50.1
pydantic==2.13.4
