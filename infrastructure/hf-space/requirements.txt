# Gradio SDK Space (ZeroGPU hardware): the platform installs gradio itself
# (sdk_version in README.md front-matter) plus fastapi/uvicorn as its deps -
# do not repin those here. torch is the standard PyPI build (ZeroGPU hosts
# have NVIDIA drivers; our inference paths run on CPU and consume no quota).
spaces==0.51.0
torch==2.12.1
einops==0.8.2
tqdm==4.68.3
safetensors==0.8.0
sentence-transformers==5.6.0
pandas
