"""Vendored Kronos foundation-model source (github.com/shiyu-coder/Kronos, MIT).

This package holds a verbatim copy of the upstream ``model/`` package (Kronos
tokenizer + model + predictor), which is not distributed on PyPI. See NOTICE.md
for provenance and the single local modification (relative import fix).

Import the runtime classes from the ``model`` subpackage:
    from app.ml.kronos_src.model import Kronos, KronosTokenizer, KronosPredictor
"""
